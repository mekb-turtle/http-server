#ifndef SERVE_FILE_H
#define SERVE_FILE_H
#include "serve.h"
// serve file data
extern enum serve_result serve_file(const struct server_config *cls, struct input_data *input, struct output_data *output);
#endif
